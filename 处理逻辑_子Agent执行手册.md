# TikTok Shop 泰国7月利润核算 - 处理逻辑

## 输入文件（7个）

| 文件 | 行数 | 用途 |
|------|------|------|
| 1. tiktok结算明细.xlsx | 2903行订单 | 核心数据源（收入+平台费+广告+佣金+物流） |
| 2. tiktok订单明细.xlsx | 3710行 | SKU映射桥梁（SKU ID → Seller SKU） |
| 3. 泰国商品资料.xlsx | 97个SKU | 成本价（CNY，含头程物流） |
| 4. 商品级推广数据.xlsx | 58个广告组 | 推广消耗/ROI（辅助参考） |
| 5. 联盟商品佣金.xlsx | 154个商品 | 达人佣金明细（辅助参考） |
| 6. 售后退款明细.xlsx | 50条 | 退货退款明细 |
| 7. 7店铺成本美妆.xlsx | 4条 | 杂项费用（CNY） |

## 核心数据处理流程

### 步骤1：建立SKU映射表

从 `tiktok订单明细.xlsx` 的 `OrderSKUList` Sheet 提取：

```
SKU ID → Seller SKU → Product Name
```

只保留 Order Status ≠ '已取消' 的记录（已取消订单不参与利润核算）。

输出：`SKU ID | Seller SKU | Product Name` 映射字典

---

### 步骤2：建立成本价映射表

从 `泰国商品资料.xlsx` 的 Sheet `截止7.22含手动头程物流版` 读取：

```
商品编码 → 商品名称 → 成本价（含头程物流版）
```

成本价单位是 **CNY（人民币）**，不是THB。

输出：`商品编码(Seller SKU) → 成本价(CNY)` 映射字典

---

### 步骤3：处理结算明细（核心步骤）

从 `tiktok结算明细.xlsx` 的 `订单详情` Sheet 读取。

**3.1 过滤：** 只取 `交易类型 = '订单'` 的行（排除"物流赔付"等调整项）。

**3.2 解析SKU：** `已售商品详情` 字段格式为 `SKU_ID * 数量;`（如 `1736250420031030678 * 1;`），部分订单该字段为 `/`。

解析逻辑：
- 如果值为 `/` 或空，跳过该订单
- 提取 `SKU ID` 和 `数量`
- 通过步骤1的映射表，将 SKU ID 转换为 Seller SKU

**3.3 字段取数规则（所有金额取绝对值，负数表示扣款）：**

| 模板列 | 结算明细源字段 | 说明 |
|--------|---------------|------|
| GMV | 享受折扣前小计 | 正数 |
| 有效销售额 | 享受商家折扣后小计 - 享受商家折扣后的退款小计 | 折扣后小计为正，退款为负（取绝对值相加） |
| 退款金额 | 享受商家折扣后的退款小计 | 取绝对值 |
| 卖家折扣 | 商家折扣 | 取绝对值（通常为负） |
| 净销售收入 | 有效销售额 - 卖家折扣 | 计算 |
| 买家付运费 | 买家支付运费 | 正数 |
| 平台佣金 | TikTok Shop 佣金费 | 取绝对值 |
| 交易手续费 | 交易手续费 | 取绝对值 |
| 平台税费及其他 | 电商增长服务费 + 基础设施费 + 其他平台费 | 取绝对值 |
| 推广费用 | GMV Max 广告费 | 取绝对值 |
| 达人佣金 | 联盟佣金 | 取绝对值 |
| 尾程物流 | 商家运费 | 取绝对值 |

**3.4 按SKU汇总：** 将上述字段按 Seller SKU 分组求和。

---

### 步骤4：汇率换算（关键步骤）

⚠️ **商品成本(CNY) → 商品成本(THB)**

```
商品成本(THB) = 成本价(CNY) × 销量 × 汇率
```

汇率建议使用 **1 CNY ≈ 5.0 THB**（7月平均汇率，如能确认实际汇率请替换）。

⚠️ **其他费用(CNY) → 其他费用(THB)**

```
其他费用(THB) = 杂项费用(CNY) × 汇率
```

---

### 步骤5：填入利润明细表

将步骤3的SKU汇总数据填入 `02-利润明细` Sheet：

- 列 B：Seller SKU
- 列 C：商品名称（从步骤1映射）
- 列 D：订单量
- 列 E：销量(件)
- 列 F：GMV
- 列 G：有效销售额
- 列 H：退款金额
- 列 J：卖家折扣
- 列 K：买家付运费
- 列 L：商品成本(THB)（步骤4换算后）
- 列 N：平台佣金
- 列 O：交易手续费
- 列 P：平台税费及其他
- 列 Q：推广费用
- 列 R：达人佣金
- 列 S：尾程物流
- 列 T：其他费用（步骤4换算后）
- 列 U：售后损失（从退款数据统计）

计算列（I / M / W / X / Y）由公式自动计算，**不需要手动填**。

---

### 步骤6：填入其他Sheet

**04-推广分析：** 从 `商品级推广数据.xlsx` 的 `Data` Sheet 直接复制到对应列。

**05-达人分析：** 从 `联盟商品佣金.xlsx` 的 `Sheet 1` Sheet 直接复制到对应列。

**06-售后分析：** 售后数据已标注（39条退货退款 + 11条仅退款），退款总额从结算明细统计。

---

## 关键注意事项

1. **币种**：结算数据 = THB，成本数据 = CNY → 必须换算
2. **取绝对值**：结算明细中扣款为负数，汇总时取绝对值
3. **已取消订单**：订单明细中 Order Status = '已取消' 的订单不参与计算
4. **已售商品详情 = '/'**：该订单无SKU明细，跳过
5. **SKU ID匹配**：结算明细的SKU ID与订单明细的SKU ID直接匹配
6. **数值格式**：原始文件中的数值可能为字符串（如 `฿1,324.21`），需先清洗再计算
7. **不假设、不编造**：所有数据从源文件取，不确定的字段留空

## 输出文件

填写完成后的 `TikTok_Shop经营利润核算表_V1.0.xlsx`，路径：
`/app/data/所有对话/主对话/TH经营数据Agent/TikTok_Shop经营利润核算表_V1.0.xlsx`